namespace demo.translate.meta_scoped_reference.example_2
version "${project.version}"

/*
$DESCRIPTION$
*/
synonym source META_SCOPED_REFERENCE_EXAMPLE_2

type Root:
    [rootType]
	priceQuantity PriceQuantity (1..1)
	product Product (1..1)

type PriceQuantity:
	quantity Quantity (1..1)
        [metadata location]
        [synonym META_SCOPED_REFERENCE_EXAMPLE_2 value "b" path "a"]

type Product:
    resolvablePayoutQuantity ResolvablePayoutQuantity (1..1)
	    [synonym META_SCOPED_REFERENCE_EXAMPLE_2 value "b" path "a"]

type ResolvablePayoutQuantity:
	resolvedQuantity Quantity (1..1)
        [metadata address "pointsTo"=PriceQuantity->quantity]
        [synonym META_SCOPED_REFERENCE_EXAMPLE_2 meta "c"] // "meta" with no "value" specified

type Quantity:
	amount number (1..1)
	    [synonym META_SCOPED_REFERENCE_EXAMPLE_2 value "c" maps 2]

/*
Schema:

$SCHEMA$
*/